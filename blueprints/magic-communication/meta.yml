# Per-blueprint metadata. `name` MUST match the folder name; it is
# used as the URL subpath (the blueprint is published at site/<name>/).
name: magic-communication
title: "Magic and Communication Complexity"
description: "Low-magic quantum circuits have low communication cost: magic lower bounds from communication, and exponential R-parallel separations."
build_pdf: false        # if true, also build + link a PDF (requires TeX in CI)
home: "https://arxiv.org/abs/2510.07246"                # optional URL shown as a "home" link on the landing card
github: ""              # optional repo URL shown as a "GitHub" link on the landing card
