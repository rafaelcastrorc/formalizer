# Per-blueprint metadata. `name` MUST match the folder name; it is
# used as the URL subpath (the blueprint is published at site/<name>/).
name: batch-codes
title: "Improved Batch Code Lower Bounds"
description: "A new Omega(sqrt(Nk)) redundancy lower bound for primitive multiset linear batch codes, via the dimension of the dual code's order-O(k) tensor."
build_pdf: false        # if true, also build + link a PDF (requires TeX in CI)
home: "https://arxiv.org/abs/2106.02163"                # optional URL shown as a "home" link on the landing card
github: ""              # optional repo URL shown as a "GitHub" link on the landing card
