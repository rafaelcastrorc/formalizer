{"contracts":[{"label":"lem:p-terms-calT","target_signature":"theorem lem_p_terms_calT (a b : ℕ) : def_p1 a b ∈ def_calT (a + 1) (b + 1) ∧ def_p2 a b ∈ def_calT (a + 1) (b + 1) ∧ def_p3 a b ∈ def_calT (a + 1) (b + 1) ∧ def_p4 a b ∈ def_calT (a + 1) (b + 1)","helpers":[],"decisions":["State all four P-term memberships in one conjunction because the blueprint node has one public Lean target.","Keep `def_calT` independent of the P-term definitions; membership is the nontrivial claim.","Parameters `a b : ℕ` index the Claim 5 transition from `T_{a,b+4}` to `T_{a+1,b+1}`.","Each `def_pi` is treated as an indexed term family `ℕ → ℕ → α` compatible with membership in `def_calT`.","Do not expose proof-only table data `def_tab` in the public signature."]},{"label":"lem:q-r-terms-calT","target_signature":"theorem lem_q_r_terms_calT (a b : ℕ) : def_q a b ∈ def_calT a (b + 2) ∧ def_r13 a b ∈ def_calT a (b + 3) ∧ def_r14 a b ∈ def_calT a (b + 3) ∧ def_r23 a b ∈ def_calT a (b + 3) ∧ def_r24 a b ∈ def_calT a (b + 3)","helpers":[],"decisions":["State the Q membership and the four R-term memberships in one conjunction for the single requested node.","Keep `def_calT` independent of `def_q` and `def_rij`; membership is the substantive assertion.","Use indices exactly as in the blueprint: Q lies in `T_{a,b+2}` and each R term lies in `T_{a,b+3}`.","Treat `def_q`, `def_r13`, `def_r14`, `def_r23`, and `def_r24` as indexed term families `ℕ → ℕ → α` compatible with `def_calT` membership.","Do not expose proof-only table data `def_tab` in the public signature."]}]}