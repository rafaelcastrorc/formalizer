{"contracts":[{"label":"lem:Pk-sum-closed","target_signature":"theorem lem_Pk_sum_closed {n k : ℕ} {I : Type} [Fintype I] (P : I → Set (EuclideanSpace ℝ (Fin n))) (hP : ∀ i : I, Pk k (P i)) : Pk k (Finset.univ.fold (fun A B ↦ minkowskiJoin A B) ∅ P)","helpers":[],"decisions":["State a genuinely finite-family closure result, not only binary closure.","Use `Pk k P` as an independent predicate from `def:Pk`; do not define `Pk` by closure under sums.","Represent the finite Minkowski sum by iterating `minkowskiJoin`, preserving dependence on `def:minkowski-join`.","The empty finite sum is represented by the fold seed `∅`; Phase 2 may need blueprint decomposition if the intended neutral polytope is different.","No convexity/polytope hypotheses are added beyond membership in `Pk`."]},{"label":"lem:full-additivity","target_signature":"theorem lem_full_additivity {n m : ℕ} {X : Set (EuclideanSpace ℝ (Fin n))} {Q : Fin m → Set (EuclideanSpace ℝ (Fin n))} (hX : Polytope X) (hQ : ∀ i : Fin m, Polytope (Q i)) (hUnion : X = ⋃ i : Fin m, Q i) : minkowskiJoin X (Finset.univ.filter (fun S : Finset (Fin m) ↦ S.Nonempty ∧ Even S.card)).fold (fun A B ↦ minkowskiJoin A B) ∅ (fun S ↦ ⋂ i ∈ S, Q i) = (Finset.univ.filter (fun S : Finset (Fin m) ↦ S.Nonempty ∧ Odd S.card)).fold (fun A B ↦ minkowskiJoin A B) ∅ (fun S ↦ ⋂ i ∈ S, Q i)","helpers":[],"decisions":["Keep `X`, the cells `Q i`, their union equation, and all intersections independent; do not encode the additivity identity into any definition.","Use all nonempty subsets of `Fin m` split by parity as the two collections `T0` and `T1`.","Do not require intersections `Q_S` to be polytopes unless Phase 2 proves or decomposes that missing hypothesis from `Polytope` and the union setting.","Both sides are Minkowski sums built from the same `minkowskiJoin` dependency used elsewhere.","The lemma remains marked semantically not-ready by planning only; the target is still the exact mathematical assertion."]},{"label":"lem:claim6","target_signature":"theorem lem_claim6 {n k : ℕ} (h : TabComputable (3 ^ n - 1) 2 k) : TabComputable 0 (3 ^ n + 2) k","helpers":[],"decisions":["Use the `def:tab` interface as the only public encoding of statements `T_{a,b}` and computability in depth `k`.","Preserve the same depth `k` in premise and conclusion.","Preserve the exact indices `3^n - 1, 2` and `0, 3^n + 2`; do not insert positivity side conditions unless forced by the existing `TabComputable` type.","The proof may use `lem_claim5`, but the statement should not assume the conclusion of Claim 5 as a field or packaged hypothesis.","Natural-number subtraction is intentional in the skeleton; Phase 2 may refine with hypotheses if `def:tab` requires them."]},{"label":"lem:claim7","target_signature":"theorem lem_claim7 {n k : ℕ} (h : TabComputable 0 (3 ^ (n - 1) + 2) (k - 1)) : TabComputable (3 ^ (n - 1)) 2 k","helpers":[],"decisions":["Use the same `TabComputable a b depth` dependency surface as Claim 6.","Preserve the one-layer depth increase from `k-1` to `k`.","Preserve the exact indices `0, 3^{n-1}+2` and `3^{n-1}, 2`.","The proof-only dependency `lem:max2-one-hidden` should justify adding one hidden layer, not be folded into `TabComputable`.","Do not add assumptions such as `n ≥ 1` or `k ≥ 1` at planning time unless the existing `def:tab` interface later forces blueprint decomposition."]}]}